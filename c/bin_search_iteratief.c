#include "math.h"

int bin_search_it_int(int rij[]) {
  int left = 0;
  int right = 0;
  int midden = 0;
  int index = -1;

  while (left != right) {
  }
  return index;
}

int main()
{
  return 0;
}

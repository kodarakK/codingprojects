#include <stdio.h>
#include "nob.h"
#include "other.c"

void main(int argc, char **argv) {
  NOB_GO_BUILD_URSELF(argc, argb);
  Nob_Cmd cmd = {0};
  nob_cmd_append(&cmd, "cc", "-Wall", "-Wextra", "-o", "main", "testing.c");
  if (!nob_cmd_run_sync(cmd)) return 1;
  return 0;
}

